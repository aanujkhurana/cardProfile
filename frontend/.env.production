# GitHub Pages is static-only — there are no serverless functions.
# Point /api/gemini and /api/sanity at the Vercel deployment where
# those functions actually live.
VITE_API_BASE=https://frontend-anuj-khuranas-projects.vercel.app